JIRAstic
========

How to use:
-----------

* Clone the repo
* Configure JIRAstic (cp config.json.dist config.json)
* Composer install
* Start server:
```
php -S localhost:8080 -t web web/index.php
```